server.error.include-message=always

# Database Configuration (MySQL)
# Uses environment variables with local defaults
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/estore_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Srinu582004}
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Hibernate & Initialization
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never

# Console disabled for production
spring.h2.console.enabled=false